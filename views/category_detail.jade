extends layout 

block content 
  h2 The details of #{cat.name} are:
  p Description: #{cat.description}
  p Items under this category are:
    ul 
      each item in items 
        li 
          a(href=item.url) #{item.name}
