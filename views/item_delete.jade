extends layout 

block content 
	p Are you sure you want to delete #{item.name}?
	form(action='' method="POST")
		div
			input(type='hidden',name='id', required='true', value=item._id )
		button(type="submit") Delete item